# Server
NODE_ENV=development
PORT=5001
API_PREFIX=/api

# CORS (comma-separated list)
# Tip: Expo Web commonly runs at http://localhost:8081
CORS_ORIGIN=http://localhost:8081,http://localhost:19000,exp://127.0.0.1:19000

# TMDB
TMDB_API_KEY=your-tmdb-api-key
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p

# Firebase Admin (pick ONE option)
# Option A: single JSON blob (must be a SINGLE LINE; multi-line JSON breaks dotenv)
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","client_email":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n"}

# Option B: base64-encoded JSON (recommended for CI/hosting env vars)
FIREBASE_SERVICE_ACCOUNT_JSON_BASE64=

# Option C: JSON file path (recommended for local dev)
FIREBASE_SERVICE_ACCOUNT_JSON_PATH=

# Option D: discrete fields (recommended for local dev if you don't want base64)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Redis (optional)
REDIS_URL=

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
SEARCH_RATE_LIMIT_MAX=40

# Logging
LOG_LEVEL=info
# Error Tracking (Optional)
# Get DSN from Sentry.io project settings
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_TRACES_SAMPLE_RATE=0.1